/** `/portal/options` — an employee who is also a shareholder sees their own grants here too. */
import { MyOptionsView } from '../../components/MyOptionsView.js';

export function Options() {
  return <MyOptionsView />;
}
