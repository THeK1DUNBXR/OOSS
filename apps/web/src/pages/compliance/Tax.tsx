import { EmptyState, PageHeader } from '../../components/ui.js';

/** Compliance — tax (docs/plan/compliance.md). */
export function ComplianceTax() {
  return (
    <>
      <PageHeader title="Tax" />
      <EmptyState message="Nothing here yet." />
    </>
  );
}
