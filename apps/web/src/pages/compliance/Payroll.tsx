import { EmptyState, PageHeader } from '../../components/ui.js';

/** Compliance — payroll (docs/plan/compliance.md). */
export function CompliancePayroll() {
  return (
    <>
      <PageHeader title="Payroll" />
      <EmptyState message="Nothing here yet." />
    </>
  );
}
