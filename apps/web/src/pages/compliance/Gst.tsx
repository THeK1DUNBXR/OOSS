import { EmptyState, PageHeader } from '../../components/ui.js';

/** Compliance — gst (docs/plan/compliance.md). */
export function ComplianceGst() {
  return (
    <>
      <PageHeader title="Gst" />
      <EmptyState message="Nothing here yet." />
    </>
  );
}
