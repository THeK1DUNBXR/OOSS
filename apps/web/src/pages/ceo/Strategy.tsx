/**
 * Chairman's Office — Strategy (docs/plan/ceo-office.md §6, Phase 0 stub).
 *
 * Replaced wholesale by the owning phase once it builds this screen.
 */
import { Card, EmptyState, PageHeader } from '../../components/ui.js';

export function Strategy() {
  return (
    <div>
      <PageHeader title="Strategy" subtitle="Vision, the three-year picture, strategic themes and the annual operating plan." />
      <Card>
        <EmptyState message="This screen is not built yet." />
      </Card>
    </div>
  );
}
