/**
 * Chairman's Office — Succession (docs/plan/ceo-office.md §6, Phase 0 stub).
 *
 * Replaced wholesale by the owning phase once it builds this screen.
 */
import { Card, EmptyState, PageHeader } from '../../components/ui.js';

export function Succession() {
  return (
    <div>
      <PageHeader title="Succession" subtitle="Readiness for every key seat, visible to the chairman only." />
      <Card>
        <EmptyState message="This screen is not built yet." />
      </Card>
    </div>
  );
}
