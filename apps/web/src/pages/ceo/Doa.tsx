/**
 * Chairman's Office — Delegation Of Authority (docs/plan/ceo-office.md §6, Phase 0 stub).
 *
 * Replaced wholesale by the owning phase once it builds this screen.
 */
import { Card, EmptyState, PageHeader } from '../../components/ui.js';

export function Doa() {
  return (
    <div>
      <PageHeader title="Delegation Of Authority" subtitle="Who may approve what, up to what ceiling." />
      <Card>
        <EmptyState message="This screen is not built yet." />
      </Card>
    </div>
  );
}
