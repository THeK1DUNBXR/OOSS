/**
 * Chairman's Office — 1:1s (docs/plan/ceo-office.md §6, Phase 0 stub).
 *
 * Replaced wholesale by the owning phase once it builds this screen.
 */
import { Card, EmptyState, PageHeader } from '../../components/ui.js';

export function OneOnOnes() {
  return (
    <div>
      <PageHeader title="1:1s" subtitle="Recurring 1:1 and skip-level conversations." />
      <Card>
        <EmptyState message="This screen is not built yet." />
      </Card>
    </div>
  );
}
