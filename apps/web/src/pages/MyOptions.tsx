/** `/me/options` — the ERP workspace view of an employee's own grants. */
import { MyOptionsView } from '../components/MyOptionsView.js';

export function MyOptions() {
  return <MyOptionsView />;
}
