/**
 * Chairman's Office — strategy detectors (docs/plan/ceo-office.md §6, Phase 0 stub).
 *
 * The owning phase replaces this file with real detector functions (the
 * `raiseException` pattern, docs/plan/ceo-office.md §2) and adds them to
 * the exported array below. `domains/ceo/detectors.ts` imports this array
 * by name and never needs to change when this file is replaced.
 */

export const detectors: Array<() => Promise<void>> = [];
