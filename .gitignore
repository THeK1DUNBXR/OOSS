node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store
apps/api/prisma/migrations/dev.db*
coverage/
.turbo/
.vite/
*.tsbuildinfo

.dev/

# Wrangler's local state and build cache.
.wrangler/

# What a browser run leaves behind: traces, screenshots, the HTML report.
e2e/test-results/
e2e/playwright-report/

# Impeccable design-workflow scratch output: review screenshots, comp mocks
# and build state are one run's evidence, not durable design record. The
# durable parts (surface briefs under .impeccable/surfaces/, DESIGN.md) stay
# tracked.
.impeccable/review/
.impeccable/mocks/
.impeccable/build/
**/.impeccable/review/
**/.impeccable/mocks/
**/.impeccable/build/

# Agent worktrees live here while a task runs; never part of the repository.
.claude/worktrees/
